version: 2

sources:
  - name: raw
    database: DBT_ECOMMERCE
    schema: RAW
    tables:
      - name: RAW_ORDERS
        description: "Raw orders data from Olist"
        columns:
          - name: ORDER_ID
            description: "Unique order identifier"
            tests:
              - unique
              - not_null
      
      - name: RAW_CUSTOMERS
      - name: RAW_ORDER_ITEMS
      - name: RAW_PRODUCTS
      - name: RAW_PAYMENTS
      - name: RAW_REVIEWS
      - name: RAW_SELLERS
      - name: RAW_GEOLOCATION
      - name: RAW_PRODUCT_TRANSLATION
